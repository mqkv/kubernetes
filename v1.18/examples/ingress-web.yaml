apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-nginx
#  namespace: default
spec:
#  tls:
#  - hosts:
#    - www.web-nginx.com
#    secretName: web-nginx-secret
  rules:
  - host: www.web-nginx.com
    http:
      paths:
      - path: /
        backend:
          serviceName: web-nginx
          servicePort: 80

